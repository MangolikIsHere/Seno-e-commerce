import Link from 'next/link'
import { catalog,money } from '@/lib/catalog'
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;const results=catalog.filter(p=>p.name.toLowerCase().includes(q.toLowerCase())||p.category.toLowerCase().includes(q.toLowerCase()));return <main className="search-page"><p className="kicker">SEARCH SENO</p><h1>Find your next<br/><em>everyday piece.</em></h1><form><input name="q" defaultValue={q} placeholder="Search products"/><button>Search</button></form><p className="search-results-count">{q?`${results.length} results for “${q}”`:'Browse all products'}</p><div className="search-results">{results.map(p=><Link href={`/products/${p.slug}`} key={p.slug}><img src={p.image} alt={p.name}/><span>{p.name}</span><strong>{money(p.price)}</strong></Link>)}</div></main>}
